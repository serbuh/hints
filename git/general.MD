C | A
-|-
`git -C <path> <command>` | Pretend you're in the <path> directory when running this command.

### Submodules
C | A
-|-
`git submodule update --remote` | Pull the latest commit in the submodule